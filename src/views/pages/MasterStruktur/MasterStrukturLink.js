// Link dummy for testing using JSON - Server

// base url
const base_url = "http://localhost:8080/struktur-non-mkt/master";

// Endpoint
const get_department = base_url + "/department";
const get_position = base_url + "/position";

export { get_department, get_position};

